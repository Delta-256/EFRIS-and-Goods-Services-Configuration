# This file is intentionally empty.
# The rclone config is injected at container startup via the RCLONE_CONF environment variable.
# See Dockerfile CMD for how it is written to /root/.config/rclone/rclone.conf at runtime.
