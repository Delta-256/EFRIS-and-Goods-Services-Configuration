# This file is a placeholder — replace with your actual rclone config
# To generate: run "rclone config" on your laptop, choose OneDrive or Google Drive
# Then paste the generated config here before building the Docker image
#
# Example OneDrive config:
# [onedrive]
# type = onedrive
# token = {"access_token":"..."}
# drive_id = ...
# drive_type = personal
#
# Example Google Drive config:
# [gdrive]
# type = drive
# token = {"access_token":"..."}
# root_folder_id = ...
